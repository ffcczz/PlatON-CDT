#pragma once

//  声明表示，表示该方法是否为可导出的  此处的定义表示在编译时将PLATON_ABI(NAME, MEMBER) 替换为空
#define PLATON_ABI(NAME, MEMBER)



namespace platon {
    class Contract {
        public:
            Contract(){}
    };
}


